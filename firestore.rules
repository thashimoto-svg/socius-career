rules_version = '2';

// Phase 1 placeholder: deny everything by default.
// Phase 5 replaces this with the real per-user rules for
// users/{uid} + sessions + messages + episodes.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
