# socius-career

## ブランチ運用

長期ブランチ `feat/firebase-migration` は 2026-08-12 に main へマージして解消した。
以後は main を常に最新とし、作業ブランチは **main から `feat/*` を切る**。
長期間 main から離れたブランチは作らず、小さく切って早くマージする。

アプリ本体の指示は `apps/web/CLAUDE.md` を参照。
